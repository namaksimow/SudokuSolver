[project]
name = "sudoku"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nikita", email = "nikita_maximov05@mail.ru" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sudoku = "sudoku:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
